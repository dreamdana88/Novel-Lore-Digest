请准备将 Novel Lore Digest 的输出同步/快捷导入到本地 SillyTavern。

你必须先读取：
1. PROJECT_CONFIG.md
2. outputs/SillyTavern世界书.md
3. outputs/SillyTavern角色汇总.md
4. prompts/11_导出SillyTavern作品角色卡.md

当前推荐方式：
- 默认导出一张可直接导入 SillyTavern 的作品角色卡 JSON。
- 角色卡名称使用 PROJECT_CONFIG.md 的作品名。
- 角色卡本体只作为容器，除 `data.name` 外，`description/personality/scenario/first_mes/mes_example` 等角色定义字段保持空字符串。
- 世界、势力、地点、剧情、时间线、关系和角色汇总全部写入内嵌 `data.character_book.entries`。
- 不直接修改 SillyTavern 存档或世界书数据库。

任务：
1. 运行 `python scripts/export_story_card_for_sillytavern.py`。
2. 检查导出报告。
3. 如果存在缺失字段、标签不闭合、空内容、重复条目名或大量占位符，请提醒用户先修正或复查。
4. 如果通过，请告诉用户导入 `outputs/sillytavern-story-card/{作品名}.json`。

旧方式：
- 只有当用户明确要求"导出世界书导入块"或"准备星辰工坊导入"时，才运行 `python scripts/export_worldbook_for_starforge.py`。
- 旧方式会生成 `outputs/star-forge-import/世界书导入块.md`，用于复制到 SillyTavern 聊天并由星辰工坊世界书管理器扫描导入。

要求：
- 不要改写世界书条目内容。
- 不要给作品角色卡添加开场白、旁白身份、主持人规则、玩法说明或示例互动。
- 不要修改 source_raw/。
- 不要直接写入本地酒馆数据。
