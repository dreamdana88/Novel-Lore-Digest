# Novel Lore Digest 配置生成器：阶段 B UI/UX 改进任务书

## 1. 任务性质

这是现有配置生成器的第二轮视觉与信息架构改进，不是重写功能，也不是增加新能力。

当前页面功能已完成，自动化测试为 47/47 PASS。第一轮视觉版本虽然整齐，但仍呈现为高密度后台表单：字号偏小、重复说明过多、导航重复、实时 Markdown 预览过强、所有内容一次展开，未达到“简洁、有文学气质、普通用户打开即可填写”的目标。

本轮目标是通过删减、放大、留白和渐进披露，把页面改成轻量的“小说档案启动页”。

## 2. 技术边界

### 允许修改

- `启动-novel-lore-digest.html`
- `tools/project-config-generator/project-config-generator.css`

### 禁止修改

- `tools/project-config-generator/project-config-generator-core.js`
- `tools/project-config-generator/project-config-generator-filesystem.js`
- `tools/project-config-generator/project-config-generator-controller.js`
- 所有 `*.test.cjs`
- `tools/project-config-generator/project-config-generator-contract.md`
- `PROJECT_CONFIG.md`
- `source_raw/`
- `workspace/`
- `outputs/`
- README、workflow、prompt 和旧向导

### DOM 契约

必须完整遵守 `tools/project-config-generator/project-config-generator-contract.md`：

- 不得删除、改名或复用任何冻结的 `id`、`name`、`data-*`。
- 不得改变脚本和样式加载顺序，不得改成 ES Module。
- 角色、特别注意、原文清单使用的 `<template>` 结构必须保持兼容。
- `btn-generate` 仍是唯一主要提交按钮，且保持 `type="submit"`。
- `btn-reset` 保持 `type="button"`。
- 显示和隐藏状态继续使用 `hidden`，不得另造状态系统。
- 用户输入不得写入 `innerHTML`。
- 可以调整视觉 class、文案、容器和布局；只要冻结接口仍然存在且行为不变。

## 3. 设计目标

### 产品定位

- 产品类型：本地离线工具、小说分析任务生成器。
- 目标用户：不熟悉 Markdown、目录权限或分析工作流的普通用户。
- 视觉关键词：现代编辑台、小说档案、温暖纸张、克制、清晰、轻盈。
- 禁止方向：企业后台、密集仪表盘、开发者控制台、花哨落地页。

### UI/UX Pro Max 基准

- 布局采用 `Minimal Single Column`。
- 密度采用 `Spacious`，使用明确的 8px 间距节奏。
- 每个页面只突出一个主要 CTA。
- 正文基准不低于 16px，辅助文字不低于 14px。
- 表单控件和主要点击区域高度不低于 44px。
- 长表单使用 progressive disclosure，不得把全部高级信息同时摊开。
- 视觉层级依靠字号、间距和对比建立，不依靠大量徽章、边框和阴影。

## 4. 必须解决的问题

### P0：删除重复的流程表达

当前页面同时存在：

1. 顶部三张步骤说明卡。
2. 四项区域导航。
3. 正文中的“阶段 01 / 03 / 04”标签。

同一个流程不得显示三遍。

改进要求：

- 页面只保留一套可见的轻量步骤提示：`填写配置 → 添加原文 → 生成任务`。
- `nld-step-guide` 必须保留，但改为紧凑的单行步骤条，不再使用三张大卡片和长说明。
- `nld-section-nav` 必须保留接口。可以作为仅在键盘聚焦时显示的辅助跳转，或以不与步骤条竞争的低优先级形式呈现。
- 删除所有纯装饰性的“阶段 XX”可见标签；相应容器和冻结 id 不得删除。
- 首屏不得先展示一屏操作说明。

### P0：首屏直接进入任务

在 1440×900、浏览器缩放 100% 时，首屏必须至少看到：

- 产品名称与一句用途说明。
- 紧凑步骤条。
- 作品名、作者。
- 作品结构类型的标题以及首组选项。

用户打开页面后应立即开始填写，而不是先阅读说明书。

### P0：降低 Markdown 预览权重

当前黑色代码预览是页面中对比最强、面积最大的元素，喧宾夺主。

改进要求：

- `preview-config` 和 `preview-import-list` 必须保留并继续实时更新。
- 完整 Markdown 默认收起，通过“查看生成内容”或类似明确控件展开。
- 收起状态只显示简短摘要，例如“预览已同步 / 尚缺 3 项”。
- 初始首屏不得出现大块黑色代码区域。
- 展开后的预览仍应易读，但不能使用纯黑高对比块压过主表单；推荐深墨蓝灰或纸张式代码面板。
- “复制 Markdown”保持次要按钮，不得与“生成任务”竞争。

### P0：使用渐进披露

默认展开并保持高优先级：

- 作品名和作者。
- 作品结构类型。
- 分析重点。
- 需要深度分析的主要角色。
- 原文导入。
- 生成动作与状态。

降低优先级或收进原生 `<details>`：

- 语言、文本来源、分析日期。
- 只需简要记录的角色。
- 暂不分析的角色。
- 特别注意。
- 输出目标细项。
- 只读分析粒度。
- 完整 Markdown 和导入清单预览。

注意：隐藏在折叠区域中的必填错误必须能被用户发现。不得把真正必填项默认折叠。

### P1：重建字号系统

采用清晰、有限的字号层级：

| 用途 | 建议字号 |
| --- | --- |
| 页面主标题 | 36–44px，响应式缩放 |
| 分区标题 | 24–28px |
| 卡片/字段组标题 | 18–20px |
| 正文和输入内容 | 16px |
| 字段标签 | 15–16px，500–600 字重 |
| 辅助文字、错误信息 | 14px |
| 极少量元数据 | 最低 12px |

不得再把 `0.72rem`、`0.78rem`、`0.8rem` 用作大面积可见说明文字。

### P1：减少说明和徽章

- 每个分区最多保留一句真正有助于决策的说明。
- 与 placeholder 重复的说明删除。
- “阶段”“默认中文”“当天”等不需要反复制作徽章。
- 必填标记统一使用一个简单符号或短文本。
- “必填，可多选”不应形成高饱和红色视觉噪声，可拆成必填标记和普通辅助文本。
- 页面上同一信息只表达一次。

### P1：改成单列主流程

- 页面主内容推荐宽度为 880–960px，居中。
- 取消长期并列的“左侧表单＋右侧 Markdown/生成”工作台布局。
- 生成动作自然出现在主流程末端。
- 大屏不能因为空间充足就恢复高密度仪表盘。
- 小屏不得出现横向滚动或嵌套滚动。

### P1：减少卡片和边框

- 不要让每一组字段都成为独立白色矩形卡片。
- 一级区域可使用柔和纸张表面；二级分组优先用留白、标题和细分隔线表达。
- 阴影只保留一个层级，不得每个选项都有卡片感。
- 复选选项可以是舒展的选择标签，但必须保持清晰的选中、聚焦和禁用状态。

### P2：建立文学编辑台气质

- 保留暖纸背景、深墨蓝正文和少量朱砂强调。
- 标题可使用离线安全的衬线字体栈；正文和表单继续使用系统无衬线字体。
- 不得依赖 Google Fonts、CDN、外部图片或网络资源。
- 不使用大渐变、玻璃拟态、霓虹、3D 卡片或装饰性动画。
- 不需要插画；视觉特色来自排版、留白和纸张层次。
- 动效只用于展开、状态变化和按钮反馈，时长 150–250ms，并支持 `prefers-reduced-motion`。

### P2：统一图标

- 删除“🚀”“📋”等结构性 Emoji。
- 能用文字表达时不加图标。
- 必须使用图标时采用同一风格的内联 SVG，不得联网加载图标库。
- 图标按钮必须有可访问名称。

## 5. 推荐页面结构

```text
Novel Lore Digest
把小说整理成 Agent 可以直接执行的分析任务

① 填写配置 ─── ② 添加原文 ─── ③ 生成任务

作品信息
[作品名                              ]
[作者                                ]
▸ 更多作品信息

作品类型
[连续长篇] [单元故事] [群像史诗] ...

本次分析重点
[角色人设] [关系网] [世界观] [剧情] ...

主要角色
[角色名                         ] [移除]
[＋ 添加主要角色]
▸ 其他角色分级

▸ 特别注意与高级设置

添加小说原文（可选）
[选择文件] [选择文件夹]
也可以自行放入 source_raw/

配置状态：还差作品名、作品类型

[生成任务]
[查看生成内容]
```

这只是信息层级示意，不要求照搬字符和排版。

## 6. 文案要求

- 语气直接、自然，不使用技术说明书口吻。
- 将“定义小说的基础档案属性、叙事结构模型以及本次提取重点”改为更直接的表达，例如“告诉 Agent 这是什么作品，以及你最想整理什么”。
- “浏览器会弹出授权窗口”只在生成按钮附近提示一次。
- `source_raw/`、`PROJECT_CONFIG.md` 等技术名称仅在确有帮助时出现。
- 危险、冲突、回滚和备份信息不得被美化文案掩盖，状态文案继续由控制器决定。
- 不改业务字段的含义，不自行增加新的作品类型、分析重点或配置项。

## 7. 可访问性与交互

- 所有原生 label、fieldset、legend 和 aria 属性保持有效。
- 键盘 Tab 顺序必须符合视觉顺序。
- 聚焦态至少有 2px 可见轮廓，不得只依赖颜色微变。
- 正常正文与背景对比度至少 4.5:1。
- 错误、成功、警告不能只靠颜色区分，必须保留文字或图形标记。
- 所有主要按钮、复选项和可点击折叠标题具有至少 44px 的点击高度。
- `disabled`、生成中、成功、警告、失败和部分写入状态必须清楚区分。
- 不得隐藏浏览器目录授权、备份、冲突和回滚反馈。

## 8. 功能回归清单

视觉完成后必须逐项确认：

- 必填缺口实时更新。
- Markdown 实时更新，收起时也必须更新。
- 三类角色可以添加、重命名、移动和删除。
- 跨组角色冲突仍能定位到对应输入项。
- 特别注意可以添加和删除。
- 输出目标和分析重点同步正常。
- 选择小说文件、选择文件夹、清空导入清单正常。
- 待导入、冲突、忽略三类清单仍能显示。
- 复制 Markdown 正常。
- 生成任务仍会请求项目根目录授权。
- 非空配置替换确认仍可操作。
- 生成中、成功、等待原文、取消、失败、回滚和部分写入状态均未丢失。
- 重置表单正常。

## 9. 自动化验收

必须在项目根目录执行并提交完整结果：

```powershell
node --test tools/project-config-generator/*.test.cjs
node --check tools/project-config-generator/project-config-generator-core.js
node --check tools/project-config-generator/project-config-generator-filesystem.js
node --check tools/project-config-generator/project-config-generator-controller.js
git diff --check
```

要求：

- 47/47 测试全部通过。
- 三个脚本语法检查通过。
- `git diff --check` 通过。
- 若测试数量变化，必须说明原因；本任务不授权修改测试。

## 10. 截图验收

Gemini 不得仅用文字声明“已美化”。必须提交以下真实浏览器截图：

1. 1440×900、100% 缩放的初始首屏。
2. 1440px 宽完整页面截图。
3. 展开“查看生成内容”后的预览截图。
4. 375×812 移动端截图。
5. 至少一张必填错误状态截图。
6. 至少一张生成成功或等待原文状态截图；若目录选择器无法自动操作，明确标为 BLOCKED，不得用静态截图冒充。

截图验收标准：

- 首屏直接出现主要输入项。
- 初始首屏没有大块黑色 Markdown。
- 没有重复的步骤卡、导航和阶段标签。
- 没有肉眼难读的小字。
- 页面只存在一个明显的主按钮。
- 移动端没有横向滚动。
- 展开和错误状态没有造成布局破裂。

## 11. 交付格式

交付时必须提供：

1. 修改文件清单。
2. 删除或合并了哪些重复信息。
3. 新字号、间距和颜色规则摘要。
4. 自动化测试输出摘要。
5. 六类截图或对应 BLOCKED 说明。
6. 明确声明未修改 JavaScript、测试、DOM 契约、配置和用户数据。

## 12. 完成定义

只有同时满足以下条件，阶段 B 才算完成：

- 页面从高密度后台表单转为低密度小说档案启动页。
- 用户无需阅读长说明即可开始填写。
- 主要内容在 100% 缩放下清楚易读。
- 高级信息和完整 Markdown 不再默认占据注意力。
- 页面具备克制而明确的文学编辑台气质，不是普通 SaaS 模板。
- 所有冻结接口保留，47 项测试通过。
- 真实截图通过人工复查。

在截图未经人工确认前，只能报告“已提交视觉候选版”，不得报告“视觉验收完成”。
